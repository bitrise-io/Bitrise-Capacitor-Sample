import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bitrise.starter',
  appName: 'Bitrise-Capacitor-Sample',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
